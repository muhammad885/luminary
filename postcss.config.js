module.exports = {
  plugins: {
    'postcss-import': {},     // Handles @import if needed
    'tailwindcss': {},
    'postcss-nested': {},     // Handles nesting like in your globals.css
    'autoprefixer': {},
  },
}
